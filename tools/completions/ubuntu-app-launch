_have ubuntu-app-launch &&
_have ubuntu-app-launch-appids &&
_ubuntu_app_launch()
{
	local cur=${COMP_WORDS[COMP_CWORD]}
	COMPREPLY=($(compgen -W "`ubuntu-app-launch-appids`" -- $cur))
} &&
complete -F _ubuntu_app_launch -o default ubuntu-app-launch
